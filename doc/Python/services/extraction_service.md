# extraction_service.py Documentation

## Location
`Python/services/extraction_service.py`

## Overview
The massive operational heart scaling the complete Python Engine natively. It effectively manages mapping the raw JSON boolean logic configurations over precisely to fully executing complex machine-learning loops.

## Key Responsibilities
- Maps human text string targets logically utilizing massive Python DI lookup dictionaries (`_EXTRACTOR_REGISTRY`, `_PREPROCESSOR_REGISTRY`).
- Hard codes logical execution sequences securely via `_PREPROCESSOR_ORDER` globally to prevent artifacts (e.g. Resampling strictly must occur universally prior to HighPass filtering).
- Implements the primary `run()` looping state natively parsing target arrays securely dynamically via `librosa`.
- Orchestrates invoking extraction subclasses dynamically over multiple files, catching and nullifying failures gracefully utilizing matrix zero-padding dynamically.
- Synchronously tracks percentage execution logic dynamically pushing updates out using a local callback lambda pointer linking directly to `main.py` state dictionaries natively.
- Packs finished mathematical CSV footprints directly overlapping into safe session-only ZIP archives recursively.
