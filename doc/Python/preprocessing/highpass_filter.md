# highpass_filter.py Documentation

## Location
`Python/preprocessing/highpass_filter.py`

## Overview
A critical Preprocessor DSP script applying classic digital physical Butterworth Highpass filtration removing aggressive mechanical sub-bass floor frequencies entirely completely safely natively.

## Key Responsibilities
- Extracts numerical cutoff ranges directly from dynamic dictionary mappings securely matching `"high_pass_hz"` JSON keys implicitly.
- Executes structural 4th order (`FILTER_ORDER = 4`) `scipy.signal.butter` models mathematically.
- Performs `sosfilt` application strictly avoiding phase tearing logically gracefully cleanly efficiently.
