# DashboardSection.java Documentation

## Location
`Java/src/main/java/com/speech/ui/DashboardSection.java`

## Overview
An interface defining the contract for modular GUI sections within the application.

## Key Responsibilities
- Forces implementing classes to provide a `JPanel` via `buildPanel()`.
- Forces implementations to declare a section title via `getSectionTitle()`.
